{
  "name": "heroboard",
  "description": "Heroboard: gamified dev board for Claude Code — MCP task tools, effort heartbeats (Monkey/Agent time), and /heroboard commands.",
  "version": "0.8.0",
  "author": { "name": "Heroboard" },
  "homepage": "https://dev.heroboard.app",
  "repository": "https://github.com/rnm-dev/heroboard-claude-code",
  "userConfig": {
    "api_key": {
      "type": "string",
      "title": "Heroboard API key",
      "description": "Get one in Heroboard → Settings → MCP → “+ New key”. Stored securely in your system keychain. In the Claude app, run the plugin in a terminal once so effort hooks can read it.",
      "sensitive": true,
      "required": true
    },
    "presence_ticker": {
      "type": "boolean",
      "title": "Continuous presence ticker",
      "description": "Keep effort accruing every ~60s while a session is active — i.e. you prompted within the last 5 min — so time tracks live presence, not just an open window. Goes idle automatically once you stop prompting. On by default; turn off to count only prompt/edit events.",
      "default": true
    }
  }
}
